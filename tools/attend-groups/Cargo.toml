[package]
name = "attend-groups"
version = "0.1.0"
edition = "2021"
description = "Shared focus-group state I/O for attend and attend-chat (ADR-170)"
license = "MIT OR Apache-2.0"

[lib]
name = "attend_groups"
path = "src/lib.rs"

[dependencies]
attend-heartbeat = { path = "../attend-heartbeat" }
