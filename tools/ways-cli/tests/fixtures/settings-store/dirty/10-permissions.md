---
scope: user
settings:
  permissions:
    allow: ["Bash(git:*)"]
  includeCoAuthoredBy: false
---
# Baseline permissions
Clean fragment — contributes no findings. Sets `includeCoAuthoredBy` so the
duplicate-scalar check has an earlier occurrence to point at.
