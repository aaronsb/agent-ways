---
scope: user
settings:
  frobnicate: true
---
# Unrecognized key
`frobnicate` is not in the curated schema. Unknown keys are a WARNING, never an
error — Claude Code version-gates ~90 keys and we must not reject valid-but-new
config.
