[package]
name = "sensor-keepwarm"
version = "0.1.0"
edition = "2021"
description = "keeps the prompt cache warm with one wake per idle stretch"
license = "MIT"

[dependencies]
sensor-trait = { path = "../sensor-trait" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
